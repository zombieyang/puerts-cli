# Puerts Npm Core

#### installProjectNativeFile(dir: string)
Install file (eg. CSharp,DLL) to your unity project.
dir is the location of your package. Which included `{puerts: {nativeFile: [{src, dest}]}}` in the package.json


